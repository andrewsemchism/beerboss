# Use a lightweight Nginx image just to serve static files
FROM nginx:alpine

# Copy your frontend Nginx config
COPY nginx.conf /etc/nginx/conf.d/default.conf

# Copy the prebuilt React static files (from your local build/)
COPY build /usr/share/nginx/html

# Optional: improve caching and compression (can be added later if needed)
